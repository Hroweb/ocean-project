import {ArrRight} from '@/components/svgs';

const CustomNextButton = () => {
    return (
        <div className="sl-nav sl-next fx fx-jc fx-ac">
            <ArrRight/>
        </div>
    );
};

export default CustomNextButton;